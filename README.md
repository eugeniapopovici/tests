# Test Project

Spring Boot application (WAR) that uses Spring Security, JPA, REST, Integration, and multiple Spring modules.  
It can run directly with Spring Boot (no external server required).

---

## How to Run
Start the application using Maven:

```bash
  mvn spring-boot:run
```

## Application URL
[`localhost`](http://localhost:8080/)